//The user will paste a value in input box
//on clicking the button that value will be stored in stack
//when the user will click the pop button , the element from the top of the stack will be removed
